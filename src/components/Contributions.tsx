/**
 * Contributions
 * note: at mount, show fullScreen, auto-close in 30 seconds
 * every 2 minutes, show single line (toggle open icon)
 */

import React, { useState } from 'react';

interface Props {
  size?: 'full' | 'line';
}

const Contributions = () => {
  return <div>Contributions</div>;
};

export default Contributions;
